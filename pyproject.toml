[project]
name = "nlpops_project"
version = "1.0.0"
description = "Zero-shot comment classification with microbatching, Prometheus, and Grafana."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.136.3",
    "prometheus-client>=0.25.0",
    "torch>=2.12.0",
    "transformers>=5.10.2",
    "uvicorn[standard]>=0.49.0",
]
